{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/assets/*", "*.{css,js,svg,png,jpg,ico,woff2}"]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://eventsync-api.azurewebsites.net/api/{*}"
    }
  ]
}